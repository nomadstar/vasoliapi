# Variables generales
PORT=3000
NODE_ENV=development

# Base de datos
# URI completa de MongoDB. Ej: mongodb://usuario:pass@host:27017/mi-db
MONGO_URI=mongodb://localhost:27017/vasoliapi
# (Opcional) nombre de la base de datos si la aplicación lo usa por separado
DB_NAME=vasoli

# Autenticación / seguridad
# Usar un secreto fuerte para tokens/JWT si la API lo necesita
JWT_SECRET=changeme_reemplazar_por_valor_fuerte

# Google Drive (OAuth2)
CLIENT_ID=your-google-client-id.apps.googleusercontent.com
CLIENT_SECRET=your-google-client-secret
# URL de callback registrada en Google Cloud (opcional, valor por defecto usado en código si no está)
REDIRECT_URI=http://localhost:3000/api/drive/oauth2callback
# (Opcional) si quiere listar un folder concreto
DRIVE_FOLDER_URL=https://drive.google.com/drive/folders/XXXXXXXXXXXXX

# SMTP (correo)
# Puede proporcionar credenciales en claro o en Base64. Si provee *_B64, el código decodificará y priorizará el valor B64.
SMTP_USER=usuario@example.com
SMTP_PASS=contrasena_smtp
SMTP_USER_B64= # base64 de SMTP_USER (opcional)
SMTP_PASS_B64= # base64 de SMTP_PASS (opcional)

# Logging / otros (opcionales)
LOG_LEVEL=info

# NOTA: No commitear credenciales reales. Copiar este archivo a .env y completar valores reales en el entorno de despliegue.